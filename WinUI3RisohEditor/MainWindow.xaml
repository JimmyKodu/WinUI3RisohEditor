<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUI3RisohEditor.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI3RisohEditor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="RisohEditor - WinUI3">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <MenuBar Grid.Row="0">
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="New" Click="OnNewFile"/>
                <MenuFlyoutItem Text="Open..." Click="OnOpenFile"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Save" Click="OnSaveFile" IsEnabled="{x:Bind HasOpenFile, Mode=OneWay}"/>
                <MenuFlyoutItem Text="Save As..." Click="OnSaveAsFile" IsEnabled="{x:Bind HasOpenFile, Mode=OneWay}"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Exit" Click="OnExit"/>
            </MenuBarItem>
            <MenuBarItem Title="Edit">
                <MenuFlyoutItem Text="Add Resource..." Click="OnAddResource" IsEnabled="{x:Bind HasOpenFile, Mode=OneWay}"/>
                <MenuFlyoutItem Text="Delete Resource" Click="OnDeleteResource" IsEnabled="{x:Bind HasSelectedResource, Mode=OneWay}"/>
                <MenuFlyoutSeparator/>
                <MenuFlyoutItem Text="Import..." Click="OnImportResource" IsEnabled="{x:Bind HasOpenFile, Mode=OneWay}"/>
                <MenuFlyoutItem Text="Export..." Click="OnExportResource" IsEnabled="{x:Bind HasSelectedResource, Mode=OneWay}"/>
            </MenuBarItem>
            <MenuBarItem Title="View">
                <MenuFlyoutItem Text="Expand All" Click="OnExpandAll"/>
                <MenuFlyoutItem Text="Collapse All" Click="OnCollapseAll"/>
            </MenuBarItem>
            <MenuBarItem Title="Help">
                <MenuFlyoutItem Text="About" Click="OnAbout"/>
            </MenuBarItem>
        </MenuBar>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Resource Tree View -->
            <Border Grid.Column="0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
                <TreeView x:Name="ResourceTreeView" 
                          SelectionChanged="OnResourceSelectionChanged">
                    <TreeView.ItemTemplate>
                        <DataTemplate>
                            <TreeViewItem ItemsSource="{Binding Children}" 
                                          Content="{Binding Name}"/>
                        </DataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Border>

            <!-- Splitter -->
            <Border Grid.Column="1" Width="4" Background="Transparent" 
                    PointerEntered="OnSplitterPointerEntered" 
                    PointerExited="OnSplitterPointerExited"/>

            <!-- Resource Editor Area -->
            <Grid Grid.Column="2" Padding="12">
                <StackPanel x:Name="EmptyStatePanel" 
                            HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            Visibility="Visible">
                    <FontIcon Glyph="&#xE8B7;" FontSize="48" Foreground="{ThemeResource SystemAccentColor}"/>
                    <TextBlock Text="No file opened" 
                               Margin="0,12,0,0" 
                               Style="{ThemeResource SubtitleTextBlockStyle}"/>
                    <TextBlock Text="Open a resource file to begin editing" 
                               Margin="0,4,0,0" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>

                <ScrollViewer x:Name="ResourceEditorPanel" 
                              Visibility="Collapsed">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Resource Editor" Style="{ThemeResource TitleTextBlockStyle}"/>
                        <TextBlock x:Name="ResourceTypeText" Text="Type: Unknown"/>
                        <TextBlock x:Name="ResourceNameText" Text="Name: Unknown"/>
                        <TextBox x:Name="ResourceContentEditor" 
                                 Header="Content" 
                                 AcceptsReturn="True" 
                                 Height="400" 
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button Content="Save Changes" Click="OnSaveResourceChanges"/>
                            <Button Content="Discard Changes" Click="OnDiscardResourceChanges" Style="{ThemeResource AccentButtonStyle}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" Padding="12,8">
            <Grid>
                <TextBlock x:Name="StatusText" Text="Ready" VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>
